# MATLAB 构建产物
slprj/
*.slxc

# MATLAB 临时文件
*.asv
*.autosave

# 仿真数据缓存（按需保留原始 .mat）
*.slxc.report

# LLM 密钥（绝不入库）
llm_key.txt

# 航路规划生成物（跑 Astar/dubins 脚本自动重建）
feasible_pts.mat
fly_planfjy.mat
raw_obs.mat

# 每次 init 由 airdate.m 重建 / 本地截图
aerodata.mat
_view_fig.png

# 资料目录下的 PDF（版权与体积，不入库）
资料/*.pdf

# 本地论文对照文件和提取缓存
/*.compare.pdf
/tmp/
